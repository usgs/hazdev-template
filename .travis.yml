sudo: false
language: php
node_js:
  - '5.6'
before_install:
  - composer self-update
  - nvm install 4.1.2
  - npm --version
  - node --version
install:
  - npm install
script:
  - grunt builddist
  - npm test
